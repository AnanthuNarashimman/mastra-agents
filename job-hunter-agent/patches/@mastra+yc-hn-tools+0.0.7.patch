diff --git a/node_modules/@mastra/yc-hn-tools/dist/hnapi.js b/node_modules/@mastra/yc-hn-tools/dist/hnapi.js
index 235d8fe..a5cd981 100644
--- a/node_modules/@mastra/yc-hn-tools/dist/hnapi.js
+++ b/node_modules/@mastra/yc-hn-tools/dist/hnapi.js
@@ -1,4 +1,4 @@
-import { createTool } from '@mastra/core';
+import { createTool } from '@mastra/core/tools';
 import { z } from 'zod';
 const ALGOLIA_API = {
     APP_ID: 'UJ5WYC0L7X',
@@ -49,8 +49,7 @@ export const searchHackerNews = createTool({
     id: 'Search Hacker News',
     description: 'Search for stories and comments on Hacker News',
     inputSchema: searchHNInputSchema,
-    execute: async ({ context, mastra }) => {
-        mastra?.logger?.info('Searching Hacker News', { query: context.query, page: context.page, type: context.type });
+    execute: async (context) => {
         const results = await searchHN(context.query, context.page || 0, context.type);
         // Transform the response to be more user-friendly
         return {
@@ -79,9 +78,7 @@ export const getTopStories = createTool({
         page: z.number().optional().default(0).describe('Page number (0-based)'),
         limit: z.number().optional().default(30).describe('Number of stories per page (max 100)'),
     }),
-    execute: async ({ context, mastra }) => {
-        mastra?.logger?.info('Fetching top HN stories', { page: context.page, limit: context.limit });
-        // For top stories, we want to sort by points in descending order
+    execute: async (context) => {
         const results = await searchHN('', // empty query to get all stories
         context.page || 0, 'story');
         return {
diff --git a/node_modules/@mastra/yc-hn-tools/dist/ycapi.js b/node_modules/@mastra/yc-hn-tools/dist/ycapi.js
index c942bc3..2762564 100644
--- a/node_modules/@mastra/yc-hn-tools/dist/ycapi.js
+++ b/node_modules/@mastra/yc-hn-tools/dist/ycapi.js
@@ -1,4 +1,4 @@
-import { createTool } from '@mastra/core';
+import { createTool } from '@mastra/core/tools';
 import { z } from 'zod';
 const BASE_URL = 'https://api.ycombinator.com/v0.1';
 async function fetchYCCompanies(params) {
@@ -22,9 +22,7 @@ export const searchCompanies = createTool({
         status: z.string().optional(),
         page: z.number().optional(),
     }),
-    execute: async ({ context, mastra }) => {
-        mastra?.logger?.debug('Fetching YC companies');
-        console.log(context);
+    execute: async (context) => {
         const params = new URLSearchParams();
         if (context.query)
             params.append('q', context.query);
@@ -43,9 +41,7 @@ export const getCompanyBySlug = createTool({
     inputSchema: z.object({
         slug: z.string(),
     }),
-    execute: async ({ context, mastra }) => {
-        mastra?.logger?.debug('Fetching YC companies');
-        console.log(context);
+    execute: async (context) => {
         const params = new URLSearchParams();
         params.append('q', context.slug);
         const response = await fetchYCCompanies(params);
@@ -58,9 +54,7 @@ export const listCompaniesByBatch = createTool({
     inputSchema: z.object({
         batch: z.string(),
     }),
-    execute: async ({ context, mastra }) => {
-        mastra?.logger?.debug('Fetching YC companies by batch');
-        console.log(context);
+    execute: async (context) => {
         const params = new URLSearchParams();
         params.append('batch', context.batch);
         const response = await fetchYCCompanies(params);
diff --git a/node_modules/@mastra/yc-hn-tools/package.json b/node_modules/@mastra/yc-hn-tools/package.json
index aab0646..6580b07 100644
--- a/node_modules/@mastra/yc-hn-tools/package.json
+++ b/node_modules/@mastra/yc-hn-tools/package.json
@@ -27,7 +27,7 @@
   "author": "",
   "license": "ISC",
   "peerDependencies": {
-    "@mastra/core": "latest",
+    "@mastra/core": "*",
     "zod": "^3.24.2"
   },
   "devDependencies": {
